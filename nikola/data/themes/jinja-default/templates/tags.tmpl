{% extends "base.tmpl" %}
{% block content %}
    <div class="postbox">
        <!--Body content-->
        <h1>{{title}}</h1>
        <ul class="unstyled">
        {% for text, link in items %}
            <li><a href="{{link}}"><span class="badge badge-info">{{text}}</span></a>
        {% endfor %}
        </ul>
        <!--End of body content-->
    </div>
{% endblock %}
