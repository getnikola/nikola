{% extends "base.tmpl" %}
{% block content %}
    <div class="postbox">
        <!--Body content-->
        <h1>{{title}}</h1>
        <ul class="unstyled bricks">
        {% for text, link in items %}
            <li><a href="{{link}}">{{text}}</a></li>
        {% endfor %}
        </ul>
        <!--End of body content-->
    </div>
{% endblock %}
