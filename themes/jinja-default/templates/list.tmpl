{% extends "base.tmpl" %}
{% block content %}
        <!--Body content-->
        <h1>{{title}}</h1>
        <ul class="unstyled">
        {% for text, link in items %}
            <li><a href="{{link}}">{{text}}</a>
        {% endfor %}
        </ul>
        <!--End of body content-->
{% endblock %}
